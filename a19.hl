// Use else-if after an if to check an alternate condition
.result
if
   .:bool:false
   .lambda
      set-value:x:@.result
         .:One
else-if
   .:bool:true
   .lambda
      set-value:x:@.result
         .:Two
