// Define a sample slot 'sample'
slots.create:sample  
   return-value:Done

// Retrieve the Hyperlambda definition of 'sample'
slots.get:sample  
// The result is the lambda nodes of 'sample', e.g. a child node [return-value] with value "Done".
