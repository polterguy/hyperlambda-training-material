// Create two dynamic slots 'alpha' and 'beta'
slots.create:alpha  
   return-value:1
slots.create:beta  
   return-value:2

// Get all dynamic slot names
slots.vocabulary
// Returns ["alpha", "beta"] (the list of all slot names defined dynamically), in addition to all other dynamic slots in the system.
