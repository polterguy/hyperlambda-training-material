// Illustrates how creating a cache item, for then to deleting it, and retrieving it after returns nothing
cache.set:cache-item-key
   expiration:5
   value:Howdy world

cache.clear
cache.get:cache-item-key