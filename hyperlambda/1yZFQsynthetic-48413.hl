
// Create a Hyperlambda snippet that executes [.lambda] if at least one condition is met. - With Python equivalent

// Executes [.lambda] if at least one condition is met.
.output
.numA:int:6
.numB:int:12
if
   or

      // Less than check
      lt:x:@.numA
         .:int:7

      // Greater than check
      mt:x:@.numB
         .:int:11
   .lambda

      // Setting [.output] to true
      set-value:x:@.output
         .:bool:true
else

   // Setting [.output] to false
   set-value:x:@.output
      .:bool:false


/*

# Python equivalent
def run_lambda_if_any_conditions_true():
    check1 = 2
    check2 = 8
    state = False
    
    if check1 < 3 or check2 > 7:
        state = True
    else:
        state = False
    
    return state

# Example usage
state = run_lambda_if_any_conditions_true()
print(state)

*/