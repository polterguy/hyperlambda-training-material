
// [and] returns true only if all arguments are true - With Python equivalent - With Python equivalent
and
   .:bool:true
   .:bool:true

/*
 * # Python equivalent
 * # [and] returns true only if all arguments are true
 * result = all([True, True])
 */


/*

# Python equivalent
# Returns True only if all arguments are True
result = all([True, True])

*/