
// Logs a fatal error - With Python equivalent
log.fatal:Fatal error we cannot recover from


/*

# Python equivalent
# Logs a fatal error
import logging

logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)

logger.fatal("Fatal error we cannot recover from")

*/