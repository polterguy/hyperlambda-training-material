
// Example: Remove all non-digit characters from a string (keep only digits). - With Python equivalent
.mixed:"Phone: 555-1234!"
strings.replace-not-of:x:@.mixed
   .:0123456789
   .:

// Result: "5551234" (only digits remain)


/*

# Python equivalent
# Remove all non-digit characters from a string (keep only digits)
mixed = "Phone: 555-1234!"
allowed_characters = "0123456789"

# Keep only allowed characters
result = ''.join(
    char for char in mixed if char in allowed_characters
)

*/