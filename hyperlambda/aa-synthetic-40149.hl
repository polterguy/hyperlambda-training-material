// Add children from multiple sources into multiple destinations – with Python equivalent
.dest1
.dest2
.src1
   foo1:bar1
.src2
   foo2:bar2
add:x:../*/.dest*
   get-nodes:x:@.src1/*
   get-nodes:x:@.src2/*

/*

# Python equivalent
# Add children from multiple sources into multiple destinations
dest1 = {}
dest2 = {}

src1 = {'foo1': 'bar1'}
src2 = {'foo2': 'bar2'}

# Add source nodes into all destination nodes
for dest in [dest1, dest2]:
    dest.update(src1)
    dest.update(src2)

*/
