
// Example: Mix in a simple inline expression (2+2 inside curly braces). - With Python equivalent
strings.mixin:"The result is {{\nmath.add\n   .:int:2\n   .:int:2\nreturn:x:-\n}}"

// Result: "The result is 4"


/*

# Python equivalent
# Example: Mix in a simple inline expression (2+2 inside curly braces).
result = 2 + 2
output_string = f"The result is {result}"

# Example usage
# output_string will be "The result is 4"

*/