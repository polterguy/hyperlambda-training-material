
// Example: Check if a string starts with a substring that is not at the beginning. - With Python equivalent

// Returns false
strings.starts-with:HelloWorld
   .:World


/*

# Python equivalent
# Example: Check if a string starts with a substring that is not at the beginning.
string = "HelloWorld"
substring = "World"
starts_with_substring = string.startswith(substring)

# Example usage
# starts_with_substring will be False

*/