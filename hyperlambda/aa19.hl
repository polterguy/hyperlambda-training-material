slots.create:mySlot  
   return-value:Hello

// Check if 'mySlot' exists
slots.exists:mySlot  
// Yields true (slot is defined)

// Check a slot name that doesn't exist
slots.exists:noSuch  
// Yields false (no such slot defined)
