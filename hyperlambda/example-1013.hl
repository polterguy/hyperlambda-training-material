/**
 * This example creates a custom slot `my.slot` that returns multiple nodes using `return-nodes`.
 * When the slot is invoked, it returns an integer and a string node.
 */
slots.create:my.slot
   return-nodes
      user-id:int:12345
      username:JohnDoe
      status:Active

// Invoking the slot
signal:my.slot


// Result below
user-id:int:12345
username:JohnDoe
status:Active
