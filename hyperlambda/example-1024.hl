/**
 * This example attempts to convert an image without specifying a different output format.
 * It should trigger an error since conversion requires a new file format.
 */
image.convert
   file:input.jpg
   output:input.jpg
