/**
 * This example resizes an image by specifying only the height (500 pixels).
 * The width is automatically adjusted to maintain the aspect ratio.
 */
image.resize
   file:/input.jpg
   height:int:500
