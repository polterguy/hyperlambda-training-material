/*
 * This Hyperlambda snippet demonstrates how to check if a child node exists.
 * 
 * 1. The `.dest` node is created with a child node named `foo`.
 * 2. The `exists` slot is used to check if there are any child nodes within `.dest`.
 * 3. The result will be `true` since `foo` exists.
 */

.dest
   foo
exists:x:-/*
