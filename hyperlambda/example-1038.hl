/**
 * This example checks if "/uploads/image.jpg" exists before moving it.
 * If it exists, it is moved to "/gallery/image.jpg".
 */
.result
io.file.exists:/uploads/image.jpg
if:x:-
   io.file.move
      from:/uploads/image.jpg
      to:/gallery/image.jpg
