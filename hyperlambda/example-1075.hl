/*
 * Stores a raw block of Hyperlambda code for later parsing or execution.
 * This string can be passed into [hyper2lambda] to transform into a lambda object that can be executed using [eval].
 */
.hl-code:@"
log.info:Hello from inside a string
math.add
   .:int:2
   .:int:3"
