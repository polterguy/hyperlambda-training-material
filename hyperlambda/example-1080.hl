/*
 * Converts Hyperlambda text into a lambda tree while preserving comments.
 * Comments are persisted as [..] nodes in the tree.
 */
.hl:@"// This logs something\nlog.info:\""Logged with comments\"""
hyper2lambda:x:-
   comments:bool:true
