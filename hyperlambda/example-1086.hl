// Demonstrating an indented comment inside a code block
.list
   item1
   item2
for-each:x:@.list/*

   // This comment describes the loop body and is indented correctly
   set-value:x:@.dp/#
      .:processed
