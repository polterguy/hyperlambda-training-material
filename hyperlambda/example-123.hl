/*
 * Retrieves the values of the second direct child of [.data] regardless of its name
 *
 * After execution the value of the [.first-value] node will be 200.
 */

.first-value
.data
   alpha:100
   beta:200
   gamma:300
set-value:x:@.first-value
   .:x:@.data/1
