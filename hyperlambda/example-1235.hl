/**
 * This example tries to crop an image by removing too many pixels,
 * causing an error since the total crop size exceeds the image dimensions.
 */
image.crop
   file:/input.jpg
   left:int:500
   right:int:500
