// Using variables with the [if] slot to evaluate a condition
.condition:bool:false
.result

if
   get-value:x:@.condition
   .lambda
      set-value:x:@.result
         .:Condition met
// Result ==> false
