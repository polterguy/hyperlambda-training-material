// Returns a structured error object with code and message using return-nodes
slots.create:build.error
   return-nodes
      error
         code:int:404
         message:Not found

execute:build.error
