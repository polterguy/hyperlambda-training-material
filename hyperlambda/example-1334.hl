// Define a slot 'pair' that returns two values as separate nodes
slots.create:pair  
   return-nodes
      first:Alpha
      second:Beta

// Invoke the 'pair' slot
signal:pair  
// After invocation, signal:pair has two child nodes: [first] = "Alpha", [second] = "Beta"
