// How can I create a loop that increments a value until it's 5?
.no:int:0

// will contain 5 nodes afterwards
.results

while
   lt
      get-value:x:@.no
      .:int:5
   .lambda
      add:x:@.results
         .
            .:Iteration
      math.increment:x:@.no
