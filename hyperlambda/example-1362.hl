// Connect to "CustomerDB" explicitly using a PostgreSQL adapter, then fetch data
data.connect:CustomerDB

   // Force use of PostgreSQL adapter for this connection
   database-type:"pgsql"
   data.read
      table:customer

      // Retrieve first 5 customers
      limit:5
