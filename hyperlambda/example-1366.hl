
// Create a Hyperlambda snippet that loops while [.stock] is above 0.

// Loops while [.stock] is above 0.
.stock:int:5
while
   gt
      get-value:x:@.stock
      .:int:0
   .lambda

      // Logging stock status.
      log.info:Stock available

      // Decrementing stock.
      math.decrement:x:@.stock
