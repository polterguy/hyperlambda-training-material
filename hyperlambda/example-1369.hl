// How can I use [and] to require two conditions to be true?
.a:bool:true
.b:bool:true
.status
if
   and
      get-value:x:@.a
      get-value:x:@.b
   .lambda
      set-value:x:@.status
         .:Both are true
