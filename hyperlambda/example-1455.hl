// Divide a number by a dynamic value and then by a static value
.dividend:int:24
.divisor:int:4

math.divide

   // fetches 24
   get-value:x:@.dividend

   // divides 24 by 4 -> result 6
   get-value:x:@.divisor

   // divides 6 by 2 -> result 3
   .:int:2
