/*
 * Sends JSON with automatic response conversion to lambda object allowing us to use lambda expressions to reference fields returned.
 */
http.post:"https://api.example.com/data"
   payload:@"{""query"": ""status""}"
   convert:true
