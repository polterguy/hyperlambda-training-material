/*
 * Uses [get-value] to pull numbers from other nodes and return the maximum.
 */
.inputs
   a:int:45
   b:int:67
   c:int:53
.max-score
set-value:x:-
   math.max
      get-value:x:@.inputs/*/a
      get-value:x:@.inputs/*/b
      get-value:x:@.inputs/*/c
