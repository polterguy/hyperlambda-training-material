// If the cache does not contain the specified key, generate a new entry.

cache.try-get:user.theme
   expiration:30
   .lambda
      return:dark-mode

// If "user.theme" is not found in cache, "dark-mode" is stored and returned.
