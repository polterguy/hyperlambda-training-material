/**
 * This example resizes an image by specifying only the width (400 pixels).
 * The height is automatically adjusted to maintain the aspect ratio.
 */
image.resize
   file:/input.jpg
   width:int:400
