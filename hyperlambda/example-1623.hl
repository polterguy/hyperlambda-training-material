/*
 * This Hyperlambda snippet retrieves all descendant nodes.
 *
 * 1. The `.data` node contains nested child nodes.
 * 2. The `get-nodes` slot is used with `**` to retrieve all descendants.
 * 3. The output will include all nested nodes.
 */

.data
   parent
      child1:val1
      child2:val2
get-nodes:x:@.data/**
