// Hyperlambda [if] checking substring
.filename:document.pdf
.file-type
if
   strings.ends-with:x:@.filename
      .:pdf
   .lambda
      set-value:x:@.file-type
         .:pdf

/* JavaScript equivalent of Hyperlambda [if] checking substring
const filename = "document.pdf";
let fileType;

if (filename.endsWith("pdf")) {
  fileType = "pdf";
}
*/