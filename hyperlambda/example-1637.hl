/*
 * This Hyperlambda snippet retrieves a value using an index.
 *
 * 1. The `.data` node has three children: `foo1:bar1`, `foo2:bar2`, and `foo3:bar3`.
 * 2. The `get-value` slot retrieves the second child (`foo2`).
 * 3. The expected output is `"bar2"`.
 */

.data
   foo1:bar1
   foo2:bar2
   foo3:bar3
get-value:x:@.data/1
