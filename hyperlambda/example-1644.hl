// Hyperlambda [if] with string comparison
.user-type:admin
.access-level
if
   eq:x:@.user-type
      .:admin
   .lambda
      set-value:x:@.access-level
         .:full

/* JavaScript equivalent of Hyperlambda [if] with string comparison
const userType = "admin";
let accessLevel;

if (userType === "admin") {
  accessLevel = "full";
}
*/