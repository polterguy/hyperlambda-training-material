/*
 * Example: Use variables for the regex pattern and replacement string.
 */
.pattern:"\\d"
.replacement:"X"
.source:"a1b2c3"
strings.regex-replace:x:@.source
   get-value:x:@.pattern
   get-value:x:@.replacement
// Result: "aXbXcX"
