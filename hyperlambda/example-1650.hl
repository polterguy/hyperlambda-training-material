/*
 * This Hyperlambda snippet checks if a node is null.
 *
 * 1. The `.data` node has a child `foo` with a `null` value.
 * 2. The `null` slot checks if `foo`'s value is `null`.
 * 3. The expected output is `true`.
 */

.data
   foo
null:x:@.data/foo
