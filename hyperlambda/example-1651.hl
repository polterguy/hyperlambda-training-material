/*
 * This Hyperlambda snippet checks if a node has a non-null value.
 *
 * 1. The `.data` node has a child `foo` with a value `"bar"`.
 * 2. The `not-null` slot checks if `foo` has a value.
 * 3. The expected output is `true`.
 */

.data
   foo:bar
not-null:x:@.data/*/foo
