// Show me Hyperlambda with data segment that joins all values of nodes being children of [.data]
.data
   foo1:bar1
   foo2:bar2
   foo3:bar3

// Concatenates all child values
strings.join:x:@.data/*

   // Notice, comments MUST be on separate lines.
   .:|