
// Example: Use variables for the target substring and replacement.
.target:world
.replacement:WORLD!
.phrase:Hello world
strings.replace:x:@.phrase
   get-value:x:@.target
   get-value:x:@.replacement

// Result: "Hello WORLD!"
