/*
 * This Hyperlambda snippet retrieves a static value using `get-first-value`.
 *
 * 1. The `get-first-value` slot is called with `"success"` as the only value.
 * 2. Since there's no alternative, `"success"` is returned.
 */

get-first-value:success
