// This example connects to the 'library' database and deletes a book record from the 'books' table where the title is '1984'.
sqlite.connect:library
   sqlite.delete
      table:books
      where
         and
            title.eq:1984
