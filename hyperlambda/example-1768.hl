
// Create a Hyperlambda script to evaluate a lambda if a condition is true using an 'or' condition, with an else clause for when the condition is not true.
if
   or
      eq:x:@SOME_EXPRESSION
         .:SOME_VALUE
      eq:x:@SOME_OTHER_EXPRESSION
         .:SOME_OTHER_VALUE
   .lambda

      // Lambda evaluates if condition is true.
else

   // Lambda evaluated if condition is NOT true.
