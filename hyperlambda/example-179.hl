// This snippet uses the 'x' type to store a Hyperlambda expression as a value, in addition to extrapolating the expression containing an inner expression.
.target
   item1:foo
.ref:item1
get-value:x:@.target/*/{@.ref}
// The Inner expression will be evaluated, and the value of the single node it points to will be used as a part of the outer expression.
