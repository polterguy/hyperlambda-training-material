/*
 * This Hyperlambda snippet checks if a non-existent node is null.
 *
 * 1. The `.data` node exists but has no `missing` child.
 * 2. The `null` slot checks if `missing` is null.
 * 3. The expected output is `true`.
 */

.data
null:x:@.data/*/missing
