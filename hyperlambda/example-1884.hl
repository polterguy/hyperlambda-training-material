// Using variables with the [and] slot to check multiple conditions
.condition1:bool:true
.condition2:bool:false

and
   get-value:x:@.condition1
   get-value:x:@.condition2

// Above [and] becomes false