// Using [switch] with numeric values to set a result for car part quantities
.quantity:int:4
.result
switch:x:@.quantity

   case:int:1
      set-value:x:@.result
         .:One car part

   case:int:4
      set-value:x:@.result
         .:Four car parts

   default
      set-value:x:@.result
         .:Unknown quantity