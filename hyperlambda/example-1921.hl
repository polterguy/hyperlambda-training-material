
// Generate a Hyperlambda snippet that copies nodes from [.input1] and [.input2] and appends them into [.output].

// Copies nodes from [.input1] and [.input2] and appends into [.output]
.input1
   itemA:dataA
   itemB:dataB
   itemC:dataC
.input2
   itemD:dataD
   itemE:dataE
   itemF:dataF
.output
add:x:@.output
   get-nodes:x:@.input1/*
   get-nodes:x:@.input2/*
