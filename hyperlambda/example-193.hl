
// Provide a Hyperlambda example where a counter counts down from 10.

// Counts down from 10.
.counter:int:10
while
   gt
      get-value:x:@.counter
      .:int:0
   .lambda

      // Logging countdown.
      log.info:Counting down

      // Decrementing counter.
      math.decrement:x:@.counter
