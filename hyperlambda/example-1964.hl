// Calculates average score per category by dividing total score by number of categories
.scores
   total:int:270
   categories:int:3
   average
set-value:x:@.scores/*/average
   math.divide
      get-value:x:@.scores/*/total
      get-value:x:@.scores/*/categories
