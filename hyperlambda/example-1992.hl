// Show me Hyperlambda endpoint that returns true if a file specified as an argument exists on the server.
.arguments
   file:string
io.file.exists:x:@.arguments/*/file
yield
   exists:x:@io.file.exists
