// While loop for 20 iterations logging a message
.no:int:0
while
   lt
      get-value:@.no
      .:int:20
   .lambda

      // Executing log inside the loop
      log.info:"Howdy from while"
      math.increment:@.no
