// Use else-if to check multiple conditions in sequence

.result
if
   .:bool:false
   .lambda
      set-value:x:../*/.result
         .:failure
else-if
   .:bool:true
   .lambda
      set-value:x:../*/.result
         .:OK
