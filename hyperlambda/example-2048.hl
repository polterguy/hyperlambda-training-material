// This snippet uses the '+' iterator to get the next sibling of a given node.
.sequence
   alpha:X
   beta:Y
   gamma:Z
get-value:x:@.sequence/*/beta/+  
// The expression finds "beta" under .sequence, then '+' returns its next sibling ("gamma"), so the get-value yields "Z".
