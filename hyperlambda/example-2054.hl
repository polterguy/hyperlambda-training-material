// Use else-if to check multiple value comparisons

.result
.src:int:2
if
   eq
      get-value:x:@.src
      .:int:1
   .lambda
      set-value:x:../*/.result
         .:OK
else-if
   eq
      get-value:x:@.src
      .:int:2
   .lambda
      set-value:x:../*/.result
         .:ERROR
