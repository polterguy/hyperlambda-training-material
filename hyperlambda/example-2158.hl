
// Provide a Hyperlambda example where a new node is inserted before 'step1' inside '.process'.

// Inserts a new node before "step1" inside ".process".
.process
   step1
insert-before:x:@.process/*/step1
   .
      init:Initialization step
