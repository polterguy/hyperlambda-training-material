/*
 * This Hyperlambda snippet retrieves the name of a node.
 *
 * 1. The `.data` node has a child `foo` with a value of `"bar"`.
 * 2. The `get-name` slot retrieves the name of the first child node.
 * 3. The expected output is `"foo"`.
 */

.data
   foo:bar
get-name:x:@.data/*
