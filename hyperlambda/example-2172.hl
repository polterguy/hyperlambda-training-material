/*
 * This Hyperlambda snippet demonstrates behavior when no nodes match.
 *
 * 1. The `.data` node exists but has no matching children.
 * 2. The `get-nodes` slot is used to retrieve nonexistent nodes.
 * 3. The output should be empty.
 */

.data
get-nodes:x:@.data/*
