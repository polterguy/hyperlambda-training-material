/*
 * This Hyperlambda snippet retrieves specific nodes by index.
 *
 * 1. The `.data` node has multiple children.
 * 2. The `get-nodes` slot retrieves only the first and third children.
 */

.data
   foo1:bar1
   foo2:bar2
   foo3:bar3
get-nodes
   get-value:x:@.data/0
   get-value:x:@.data/2
