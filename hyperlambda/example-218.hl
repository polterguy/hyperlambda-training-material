/*
 * Invalid: Tries to retrieve a node named "missing" that doesn't exist,
 * and uses its value without checking if it exists.
 *
 * This Hyperlambda will not throw an exception, but will not change any nodes either.
 */

set-value:x:@.result
   .:x:@.missing
