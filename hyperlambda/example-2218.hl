
// Generate a Hyperlambda snippet that simulates a database call using [fork].

// Demonstrates a simulated database call in a fork.
fork
   sleep:2000
   log.info:Database query completed
log.info:Processing user request...
