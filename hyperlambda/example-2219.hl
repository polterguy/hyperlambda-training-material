// Returns ALL items (limit is -1) from table1 where field1 equals 'howdy' or field2 equals 'world'
sql.read
   table:table1
   limit:-1
   where

      or
         field1.eq:howdy
         field2.eq:world