// This example connects to the 'kingdom' database and retrieves the oldest castle age from the 'castles' table.
// The result is returned as a single scalar value representing the age of the oldest castle.

sqlite.connect:kingdom
   sqlite.scalar:select min(age) from castles