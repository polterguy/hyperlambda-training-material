
// Write a Hyperlambda snippet that pushes elements into the designated node's children collection.

// Pushes elements into the designated node's children collection.
.container
add:x:@.container
   .
      name:description
