
// Generate a Hyperlambda snippet that dynamically concatenates a first and last name.

// Example of concatenating first and last name dynamically.
.first_name:Alice
.last_name:Johnson
strings.concat
   get-value:x:@.first_name
   .:" "
   get-value:x:@.last_name
