
// Generate a Hyperlambda snippet that embeds multiple structured data entries into a given storage node.

// Embeds multiple structured data entries into a given storage node.
.storage
add:x:@.storage
   .
      itemAlpha:detailAlpha
      itemBeta:detailBeta
      itemGamma:detailGamma
