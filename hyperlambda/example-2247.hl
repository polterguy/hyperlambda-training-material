// Create an endpoint that check if the user is a grown up
.arguments
   age:int
if
   mt:x:@.arguments/*/age
      .:int:17
   .lambda
      return
         is_grownup:bool:true
return
   is_grownup:bool:false
