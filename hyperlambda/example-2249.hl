
// Provide a Hyperlambda example where nodes from [.origin] are moved and integrated into [.destination_node].

// Moves nodes from [.origin] and integrates them into [.destination_node]
.origin
   field1:content1
   field2:content2
   field3:content3
.destination_node
add:x:@.destination_node
   get-nodes:x:@.origin/*
