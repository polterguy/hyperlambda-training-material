// Using [and] with function calls to evaluate conditions
.arg1:Hello
.arg2:World
.result

if
   and
      strings.contains:x:@.arg1
         .:Hello
      strings.starts-with:x:@.arg2
         .:World
   .lambda
      set-value:x:@.result
         .:Both conditions are true