// Demonstrating nested [if] slots
.result
if
   .:bool:true
   .lambda
      if
         .:bool:false
         .lambda
            set-value:x:@.result
               .:Nested condition met
// Condition is not true