
// Provide a Hyperlambda example where a set of order IDs is iterated and logged.

// Iterates through a set of order IDs and logs them.
.orders
   order1:int:1001
   order2:int:1002
   order3:int:1003
for-each:x:@.orders/*

   // Logging each order ID.
   log.info:x:@.dp/#
