
// Write a Hyperlambda interceptor that logs incoming requests, including the method, path, and IP.
log.info:Request received
   method:x:@request.scheme
   path:x:@request.url
   ip:x:@request.ip
.interceptor
