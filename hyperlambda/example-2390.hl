
// Generate a Hyperlambda snippet that inserts a new node after 'item1' inside '.list'.

// Inserts a new node after "item1" inside ".list".
.list
   item1
insert-after:x:@.list/*/item1
   .
      item2:Inserted after item1
