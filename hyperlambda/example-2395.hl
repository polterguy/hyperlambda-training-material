
// Write a Hyperlambda snippet that inserts a 'middle' node after 'start' inside '.sequence'.

// Inserts a "middle" node after "start" inside ".sequence".
.sequence
   start
insert-after:x:@.sequence/*/start
   .
      middle:Intermediate step
