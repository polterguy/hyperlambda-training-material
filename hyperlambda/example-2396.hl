
// Create a Hyperlambda while loop that continues to execute as long as a condition 'continue' is true. Log a message for each iteration.
while
   eq:x:@.continue
      .:bool:true
   .lambda

      // Condition is true.
      log.info:Condition is still true, iterating once more
