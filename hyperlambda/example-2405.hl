
// Generate a Hyperlambda snippet that inserts a new node before 'item1' inside '.list'.

// Inserts a new node before "item1" inside ".list".
.list
   item1
insert-before:x:@.list/*/item1
   .
      item0:Inserted before item1
