
// Provide a Hyperlambda example where structured nodes are inserted into the referenced endpoint's children.

// Inserts structured nodes into the referenced endpoint's children.
.endpoint
add:x:@.endpoint
   .
      paramX:settingX
      paramY:settingY
      paramZ:settingZ
