
// Generate a Hyperlambda snippet that duplicates nodes from [.registry] and inserts them into [.storage].

// Duplicates nodes from [.registry] and inserts them into [.storage]
.registry
   entryFirst:infoFirst
   entrySecond:infoSecond
   entryThird:infoThird
.storage
add:x:@.storage
   get-nodes:x:@.registry/*
