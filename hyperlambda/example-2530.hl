
// Write a Hyperlambda GET endpoint that returns server information like IP, host, and request IP.
server.ip
request.ip
request.host
yield
   server-ip:x:@server.ip
   request-ip:x:@request.ip
   host:x:@request.host
