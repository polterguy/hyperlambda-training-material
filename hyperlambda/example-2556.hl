// How to create an expression that retrieves a single node from input arguments

get-name:x:@.arguments/*/SOME_NODE_HERE

/*
 * And expression is ALWAYS type declares as `:x:`, implying the following
 * parts will be interpreted as an expression and parsed as such.
 */