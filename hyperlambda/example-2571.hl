
// Provide a Hyperlambda example where a new node is inserted after 'step1' inside '.process'.

// Inserts a new node after "step1" inside ".process".
.process
   step1
insert-after:x:@.process/*/step1
   .
      step2:Next process step
