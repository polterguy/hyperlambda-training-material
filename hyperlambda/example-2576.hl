
// Write a Hyperlambda snippet that inserts a 'log2' entry after 'log1' inside '.logs'.

// Inserts a "log2" entry after "log1" inside ".logs".
.logs
   log1
insert-after:x:@.logs/*/log1
   .
      log2:Next log entry
