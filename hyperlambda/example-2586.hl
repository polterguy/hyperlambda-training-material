
// Write a Hyperlambda snippet that inserts a 'log0' entry before 'log1' inside '.logs'.

// Inserts a "log0" entry before "log1" inside ".logs".
.logs
   log1
insert-before:x:@.logs/*/log1
   .
      log0:Previous log entry
