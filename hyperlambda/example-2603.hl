/**
 * This example moves "/exports/report.csv" to "/archives/report.csv".
 * If the destination file exists, it will be replaced.
 */
io.file.move
   from:/exports/report.csv
   to:/archives/report.csv
