
// Define a Hyperlambda POST endpoint that accepts a JSON payload with 'username' and 'email', and returns them in the response.
.arguments
   username:string
   email:string
yield
   message:User created successfully
   username:x:@.arguments/*/username
   email:x:@.arguments/*/email
