
// Create a Hyperlambda PATCH endpoint that updates a user's email given 'user_id' and 'email' arguments.
.arguments
   user_id:int
   email:string
yield
   message:Email updated successfully
   user_id:x:@.arguments/*/user_id
   new_email:x:@.arguments/*/email
