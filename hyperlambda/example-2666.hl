
// Create a Hyperlambda script to return the Magic CAPTCHA JavaScript library.
.arguments
   v:string
add:x:./*/execute-file
   get-nodes:x:@.arguments/*
execute-file:/system/misc/magic-captcha.js.get.hl
return:x:-
