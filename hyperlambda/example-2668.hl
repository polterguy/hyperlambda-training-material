// Processing each item in a collection
.items
   .:int:5
   .:int:10
   .:int:15
.sum:int:0
for-each:x:@.items/*
   math.add:x:@.sum
      get-value:x:@.dp/#
   set-value:x:@.sum
      get-value:x:@math.add