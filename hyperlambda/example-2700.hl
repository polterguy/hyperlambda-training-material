
// Create a Hyperlambda for-each loop that iterates through a list of items and logs the current item.
for-each:x:@.data/*

   // Currently iterated node can be found by reference in [.dp] node
   log.info:x:@.dp/#
