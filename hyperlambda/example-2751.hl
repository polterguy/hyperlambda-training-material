/*
 * This Hyperlambda snippet checks if a node has no children.
 * 
 * 1. The `.dest` node is created without any children.
 * 2. The `exists` slot is used to check for child nodes.
 * 3. The result will be `false` since there are no children in `.dest`.
 */

.dest
exists:x:-/*
