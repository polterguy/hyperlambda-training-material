
// Create a Hyperlambda script to connect to a POP3 server, retrieve all emails, and create a log entry for each email.
mail.pop3.fetch
   .lambda

      /*
       * Lambda object evaluated once for every
       * email fetched from POP3 server.
       */
      log.info
         lambda2hyper:x:./.
