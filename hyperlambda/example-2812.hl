/*
 * Example: Allow letters and spaces only (remove punctuation by replacing with empty).
 */
.phrase:"Hello, World!"
strings.replace-not-of:x:@.phrase
   .:"ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz "
   .:""
// Result: "Hello World" (comma and '!' removed)
