
// Create a Hyperlambda script to create a MIME message from a structured lambda object and parse it back to lambda.
mime.create:multipart/mixed
   entity:text/plain
      filename:/README.md
   entity:text/plain
      content:Bar
mime.parse:x:-
