
// Create a Hyperlambda action to return the originating IP address of an HTTP request.
.arguments
.icon:http

// Retrieving IP address from equest.
request.ip

// Returning IP address to caller.
yield
   ip:x:@request.ip
