
// Create a Hyperlambda action to validate that a specified 'email' is a valid email address. Validate the mandatory field.
.arguments
   email
      type:string
      mandatory:bool:true
.icon:shield

// Invoking validator.
validators.email:x:@.arguments/*/email
