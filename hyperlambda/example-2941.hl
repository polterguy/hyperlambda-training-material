// Defines a custom slot that enforces 'admin' role before continuing.

slots.create:admin.only.action
   auth.ticket.verify:admin

   // Admin-only logic executed here ...

// Usage:
execute:admin.only.action
