// Define a slot that returns two pieces of data
slots.create:getPair
   return-nodes
      key:Value  
      number:int:123

// Invoke 'getPair'
signal:getPair
// The result has child nodes: key = "Value" and number = 123
