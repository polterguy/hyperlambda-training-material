/*
 * Converts a Hyperlambda string into an executable lambda object.
 * Useful for dynamically parsing and executing raw Hyperlambda.
 */
.hl:@"log.info:""This is logged from text"""
hyper2lambda:x:-

// This executes the above node
eval:x:-
