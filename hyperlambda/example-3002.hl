// Hyperlambda [if] with less than comparison
.items-left:int:5
.restock-alert
if
   lt:x:@.items-left
      .:int:10
   .lambda
      set-value:x:@.restock-alert
         .:Inventory low!

/* JavaScript equivalent of Hyperlambda [if] with less than comparison
const itemsLeft = 5;
let restockAlert;

if (itemsLeft < 10) {
  restockAlert = "Inventory low!";
}
*/