
// Returns all plans in the system.
data.connect:[generic|hub]
   data.read
      table:plans
      columns
         name
         description
         cpu
         memory
         storage
         price
         price_reference
   return:x:-/*
