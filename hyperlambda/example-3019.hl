/*
 * Example: Pass arguments to the inline Hyperlambda code.
 */
strings.mixin:@"2 + val equals {{
math.add
   .:int:2
   get-value:x:@.arguments/*/val
return:x:-
}}"
   val:int:5
// Result: "2 + val equals 7"
