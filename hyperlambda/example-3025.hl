
// Create a Hyperlambda if/else condition that checks a variable 'foo'. Log a message if the condition is true and a different message if the condition is false.
if
   eq:x:@.arguments/*/foo
      .:bool:true
   .lambda

      // Condition is true.
      log.info:Condition is true
else

   // Condition is false
   log.info:Condition is false
