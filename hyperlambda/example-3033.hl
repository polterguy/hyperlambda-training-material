// Storing a message in the cache with a 10-second expiration time.

cache.set:session.message
   expiration:10
   value:Welcome back, user!

// This will store "Welcome back, user!" in cache under "session.message".
