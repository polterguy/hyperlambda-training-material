// Switching based on a value inside an anonymous lambda object, returning size of [.value]
.lambda
   .value:medium
   switch:x:@.value
      case:small
         return:Size is small
      case:medium
         return:Size is medium
      case:large
         return:Size is large
      default
         .result:Unknown size
invoke:x:@.lambda