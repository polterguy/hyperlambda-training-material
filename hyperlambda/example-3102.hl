
// Create a Hyperlambda script to illustrate using a for-each loop to iterate over a node-set and log each item.
.data
   item1:Hello from Item1
   item2:Hello from Item2
   item3:Hello from Item3
for-each:x:@.data/*
   log.info:x:@.dp/#
