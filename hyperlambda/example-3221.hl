/*
 * This Hyperlambda snippet retrieves the value of a node.
 *
 * 1. The `.data` node has a child `foo` with the value `"bar"`.
 * 2. The `get-value` slot retrieves the value of `foo`.
 * 3. The expected output is `"bar"`.
 */

.data
   foo:bar
get-value:x:@.data/*/foo
