/*
 * Accesses a setting from a user-specific preferences section.
 *
 * The expression dynamically resolves the user's ID from `.arguments/*/userId`,
 * turning the path into something like: `@.preferences/*/u123/*/theme`
 *
 * Ideal for multi-user preference handling with per-user customization.
 */
.user-preference:x:@.preferences/*/{@.arguments/*/userId}/*/theme
