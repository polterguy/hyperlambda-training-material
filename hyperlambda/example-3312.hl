/*
 * Retrieves the `startDate` from an event object referenced by entries in a calendar.
 * The `event` node in `.calendar/*` contains a reference to another node structure.
 * The expression dereferences it and selects its `startDate` child.
 */
.calendar-start:x:@.calendar/*/event/#/*/startDate
