/*
 * This example connects to the 'galaxy' database and retrieves the total number of planets from the 'planets' table.
 * It uses the sqlite.scalar slot to execute a SQL query that counts the number of records in the 'planets' table.
 * The result is returned as a single scalar value representing the total count of planets.
 */

sqlite.connect:galaxy
   sqlite.scalar:select count(*) from planets