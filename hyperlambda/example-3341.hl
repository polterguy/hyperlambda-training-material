// Using [switch] with boolean values to determine if a car part is available
.available:bool:true
.result
switch:x:@.available

   case:bool:true
      set-value:x:@.result
         .:Car part is available

   case:bool:false
      set-value:x:@.result
         .:Car part is not available