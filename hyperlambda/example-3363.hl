// Encrypt a message using AES algorithm, for then to decrypt it
.message:This is a secret message
crypto.aes.encrypt:x:-
   password:some-secret-password
crypto.aes.decrypt:x:-
   password:some-secret-password
