// This snippet uses the '.' iterator to retrieve the parent of a node.
.parent
   child:value
get-name:x:@.parent/*/child/.  
// The expression @.parent/*/child finds the 'child' node, then '.' gets its parent node. get-name returns ".parent".
