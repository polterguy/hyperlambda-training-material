/*
 * Dynamically targets a node in a data list using an index or dynamic label.
 *
 * If `.target-node` contains `"item42"`, the full expression becomes:
 * `@.data-list/*/item42/*/status`
 *
 * Great for generic list processing where target identifiers are computed or user-provided.
 */
.dynamic-item-status:x:@.data-list/*/{@.target-node}/*/status
