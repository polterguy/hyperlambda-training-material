
// Provide a Hyperlambda example where a set of nodes is inserted into the referenced destination expression.

// Inserts a set of nodes into the referenced destination expression.
.endpoint
add:x:@.endpoint
   .
      param:setting
