
// Generate a Hyperlambda snippet that adds multiple nodes into the specified target's children collection.

// Adds multiple nodes into the specified target's children collection.
.target
add:x:@.target
   .
      entry1:data1
      entry2:data2
      entry3:data3
