
// Create a Hyperlambda snippet that appends several key-value pairs into the given result node.

// Appends several key-value pairs into the given result node.
.result
add:x:@.result
   .
      keyA:valA
      keyB:valB
      keyC:valC
