
// Write a Hyperlambda snippet that pushes a collection of nodes into the specified container.

// Pushes a collection of nodes into the specified container.
.container
add:x:@.container
   .
      fieldOne:contentOne
      fieldTwo:contentTwo
      fieldThree:contentThree
