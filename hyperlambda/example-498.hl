
// Connect to the "sakila" database using the default connection string (SQLite) and retrieve actors (default adapter from config)
data.connect:sakila
   data.read

      // Reads from the "actor" table (returns first 25 records by default)
      table:actor
