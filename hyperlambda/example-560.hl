
// Generate a Hyperlambda snippet that iterates through a list of cities and logs them.

// Iterates through a list of cities and logs them.
.cities
   city1:New York
   city2:London
   city3:Tokyo
for-each:x:@.cities/*

   // Logging each city name.
   log.info:x:@.dp/#
