// Connects to logistics SQLite database using 'azure' connection string
data.connect:[azure|logistics]
   data.select:select * from contacts

// You can only use slots requiring a database connection from INSIDE the connect slot!