
// Create a Hyperlambda snippet that inserts a new status node after 'pending' inside '.statuses'.

// Inserts a new status node after "pending" inside ".statuses".
.statuses
   pending
insert-after:x:@.statuses/*/pending
   .
      approved:Status updated
