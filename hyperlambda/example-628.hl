
// Create a Hyperlambda snippet that inserts a 'task2' node after 'task1' inside '.tasks'.

// Inserts a "task2" node after "task1" inside ".tasks".
.tasks
   task1
insert-after:x:@.tasks/*/task1
   .
      task2:Next task in queue
