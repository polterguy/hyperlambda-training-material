/*
 * Accesses the `price` field from a product object referenced by each cart item.
 * The `product` node under `.cart/*/items/*` contains a value that is a reference to a product node.
 * This expression follows that reference and returns the `price` child of the referenced node.
 */
.price:x:@.cart/*/items/*/product/#/*/price
