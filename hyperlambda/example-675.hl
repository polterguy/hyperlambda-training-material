/*
 * Fetches the `city` node from a customer’s address object via reference.
 * Each `.customers/*/addressRef` node contains a string reference to an address node.
 * Dereferencing it using `#` allows access to that object’s `city` field.
 */
.customer-city:x:@.customers/*/addressRef/#/*/city
