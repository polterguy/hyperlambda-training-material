// Complex condition, where either field1 must be 'howdy' or field2 must be 'world' in addition to field3 being 'dudes'
sql.read
   table:table1
   limit:-1
   where

      or
         field1.eq:howdy

         and
            field2.eq:world
            field3.eq:dudes