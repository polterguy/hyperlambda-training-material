// Executing the Hyperlambda changes the node hierarchy

// This Hyperlambda
.destination
set-value:x:@.destination
   .:value

// Creates this result
.destination:value
set-value:x:@.destination
   .:value
