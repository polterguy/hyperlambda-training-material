// Show me all different ways we can create a string, with example data

// Single quotes
.foo:'howdy world this is a string'

// Double quotes
.foo:"Howdy world, another string\nNew line here ..."

// Multiline strings
.foo:@"Notice how the new line doesn't end the string
    here!"