// Can I build an [or] condition with slot results?
.a:bool:false
.b:bool:true
.decision
if
   or
      get-value:x:@.a
      get-value:x:@.b
   .lambda
      set-value:x:@.decision
         .:Passed via OR
