// String formats the value of [.foo] with the specified culture
.foo:decimal:57
format:x:-
   pattern:"{0:0.00}"
   culture:nb-NO

// The [format] slot is just a wrapper around `String.Format` from C#