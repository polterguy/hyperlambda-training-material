// Sends a support ticket to a remote API using HTTP POST
http.post:"https://api.example.com/support/tickets"
   convert:true
   payload
      subject:Login issue
      description:"I can't log in to my account."
      priority:int:1
      urgent:bool:true
