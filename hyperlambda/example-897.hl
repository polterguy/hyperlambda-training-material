// Named sibling not finding its node
.data
   not-found:value
get-value:x:@not-found

/*
 * The above named sibling iterator will NOT find the [not-found] node, since
 * it is not a direct ancestor or sibling of its direct ancestors.
 *
 * Value of [get-value] will be null after executing the above code
 */