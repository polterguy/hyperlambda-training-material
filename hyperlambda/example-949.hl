// How do I determine if a medical test result is normal using [if]?
.testResult:int:87
.result
if
   lt
      get-value:x:@.testResult
      .:int:100
   .lambda
      set-value:x:@.result
         .:Normal range
