// How do I evaluate seasonal discount eligibility with [if]?
.month:string:December
.discount
if
   eq
      get-value:x:@.month
      .:December
   .lambda
      set-value:x:@.discount
         .:Holiday 20% off
