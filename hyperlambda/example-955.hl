// A mysterious force renames all entries in the archive.

.archive
   record1:alpha
   record2:beta
   record3:gamma

for-each:x:@.archive/*
   set-value:x:@.dp/#
      .:classified

// All records in .archive are now labeled "classified".
