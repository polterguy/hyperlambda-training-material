// How do I use [or] to pass a condition if any one is true?
.condition1:bool:false
.condition2:bool:true
.result
if
   or
      get-value:x:@.condition1
      get-value:x:@.condition2
   .lambda
      set-value:x:@.result
         .:At least one is true
