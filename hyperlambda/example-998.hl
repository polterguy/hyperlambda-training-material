// Among the chosen few, we must find "Neo" in the prophecy.

.matrix
   candidate1:Morpheus
   candidate2:Neo
   candidate3:Trinity

get-value:x:@.matrix/*/candidate2

// The result is "Neo" because he is the chosen one.
