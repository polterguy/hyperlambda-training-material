/*
 * Retrieves a translation string from the `.translations` node based on a dynamic language code.
 *
 * For example, if `.language` contains `"fr"`, the full expression becomes:
 * `@.translations/*/fr/*/greeting`
 *
 * Perfect for dynamic internationalization and localization.
 */
.translated-greeting:x:@.translations/*/{@.language}/*/greeting
