/*
 * Retrieves the `title` node from a referenced article object.
 * Each `.results/*/ref` node contains a reference (value) to another node structure.
 * The `#` dereferences that value, and `/*/title` navigates to its child node named `title`.
 */
.article-title:x:@.results/*/ref/#/*/title
