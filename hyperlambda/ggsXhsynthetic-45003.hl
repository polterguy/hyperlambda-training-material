
// Hash password for then to verify it - With Python equivalent
crypto.password.hash:SomePasswordHere
crypto.password.verify:SomePasswordHere
   hash:x:@crypto.password.hash


/*

# Python equivalent
# Hash password for then to verify it
import bcrypt

password = "SomePasswordHere"
hashed_password = bcrypt.hashpw(password.encode('utf-8'), bcrypt.gensalt())

# Verify the password
is_verified = bcrypt.checkpw(password.encode('utf-8'), hashed_password)

*/