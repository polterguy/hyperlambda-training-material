
// Example: Find all digit sequences in a string. - With Python equivalent
strings.matches:abc123xyz45
   .:"\\d+"

/*
 * Resulting matches:
 * .:123
 * .:45
 */


/*

# Python equivalent
import re

# String containing occurrences of "foo"
text = "Foo foo FOO"

# Find case-insensitive occurrences of "foo"
matches = re.findall(r"(?i)foo", text)

*/