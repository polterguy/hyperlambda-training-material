
// Example: Use variables for string and trim characters (start only). - With Python equivalent
.text:...Hello
.trimchars:.

// Result: "Hello"
strings.trim-start:x:@.text
   get-value:x:@.trimchars


/*

# Python equivalent
# Use variables for string and trim characters (start only)
text = "...Hello"
trimchars = "."

# Custom implementation to mimic trim-start
trimmed_text = text
while trimmed_text.startswith(trimchars):
    trimmed_text = trimmed_text[len(trimchars):]

*/