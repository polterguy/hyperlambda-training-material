
// Example: Use a variable for the regex pattern. - With Python equivalent
.pattern:[A-Z]+
source:abcDEFghi
strings.matches:x:@.source
   get-value:x:@.pattern

/*
 * Resulting matches:
 * .:DEF
 */


/*

# Python equivalent
# Calculate the result of 2 + 2
result = 2 + 2

# Mix the result into a string
mixed_string = f"The result is {result}"

*/