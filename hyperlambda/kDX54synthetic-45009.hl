
// Change HTTP headers - With Python equivalent
response.headers.set
   Content-Type:text/plain


/*

# Python equivalent
# Change HTTP headers
from flask import Flask, Response

app = Flask(__name__)

@app.route('/')
def index():
    response = Response("Hello, World!")
    response.headers['Content-Type'] = 'text/plain'
    return response

# To run the Flask app, use: app.run()

*/