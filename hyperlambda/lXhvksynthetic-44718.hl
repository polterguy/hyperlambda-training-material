
// Example: Embed multiple inline expressions in a single string. - With Python equivalent
strings.mixin:@"Sum {{
math.add
   .:int:1
   .:int:2
return:x:-
}} and Product {{
math.multiply
   .:int:3
   .:int:4
return:x:-}}"

// Result: "Sum 3 and Product 12"


/*

# Python equivalent
# Embed multiple inline expressions in a single string
sum_result = 1 + 2
product_result = 3 * 4
output_string = f"Sum {sum_result} and Product {product_result}"

*/