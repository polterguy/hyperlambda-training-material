
// Example: Check if a string ends with a specific substring (positive case). - With Python equivalent

// Returns true
strings.ends-with:WelcomeHome
   .:Home


/*

# Python equivalent
# Original string
string = "HelloWorld"

# Substring to check
substring = "Universe"

# Check if the string ends with the substring
ends_with_substring = string.endswith(substring)

*/