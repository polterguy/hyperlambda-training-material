
// Example: Check if a string ends with a substring that is not at the end. - With Python equivalent

// Returns false
strings.ends-with:HelloWorld
   .:Universe


/*

# Python equivalent
# Original input string
input_string = "foo bar"

# Substring to replace and its replacement
old_substring = "baz"
new_substring = "qux"

# Attempt to replace the substring (no change occurs)
modified_string = input_string.replace(old_substring, new_substring)

*/