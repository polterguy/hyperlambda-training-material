
// Example: Replace a substring that is not present (no change occurs). - With Python equivalent
.input:foo bar
strings.replace:x:@.input
   .:baz
   .:qux

// Result: "foo bar" (unchanged)


/*

# Python equivalent
# Original string
string = "Hello"

# Substring to check (case-sensitive)
substring = "LLO"

# Check if the string ends with the substring (case-sensitive)
ends_with_substring = string.endswith(substring)

*/