
// Example: Check if a string ends with itself. - With Python equivalent

// Returns true
strings.ends-with:Test
   .:Test


/*

# Python equivalent
# Original string
string = "HelloWorld"

# Substring to check
substring = "Hello"

# Check if the string starts with the substring
starts_with_substring = string.startswith(substring)

*/