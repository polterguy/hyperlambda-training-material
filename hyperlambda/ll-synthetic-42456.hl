
// Example: Check if a string starts with a specific substring (positive case). - With Python equivalent

// Returns true
strings.starts-with:HelloWorld
   .:Hello


/*

# Python equivalent
# Original string
string = "HelloWorld"

# Substring to check
substring = "World"

# Check if the string starts with the substring
starts_with_substring = string.startswith(substring)

*/