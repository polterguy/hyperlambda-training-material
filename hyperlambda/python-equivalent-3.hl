// Hyperlambda Example 3: If with shorthand syntax

.arguments
   enabled:bool:true
.result

if:x:@.arguments/*/enabled
   set-value:x:@.result
      .:Feature enabled

/* Python Equivalent
arguments = {"enabled": True}
result = None
if arguments["enabled"]:
    result = "Feature enabled"
*/