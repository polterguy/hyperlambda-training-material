
// Append multiple new child nodes to a destination node - With Python equivalent
.dest
add:x:@.dest
   .
      foo1:hello
      foo2:world
   .
      bar1:hi
      bar2:planet


/*

# Python equivalent
# Append multiple new child nodes to a destination node
dest = {}
dest.update({
    'foo1': 'hello',
    'foo2': 'world'
})
dest.update({
    'bar1': 'hi',
    'bar2': 'planet'
})

*/