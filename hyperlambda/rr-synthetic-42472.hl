
// Example: Replace all digits in a string with asterisks. - With Python equivalent
.text:ID 0987
strings.regex-replace:x:@.text
   .:"\\d"
   .:*

// Result: "ID ****"


/*

# Python equivalent
import re

# Original input
input_string = "Yes yes YES"

# Perform a case-insensitive replacement of 'yes' with 'no'
modified_string = re.sub(r"(?i)yes", "no", input_string)

*/