
// Example: Replace only whole word matches using word boundaries. - With Python equivalent
.phrase:apple apples applepie
strings.regex-replace:x:@.phrase
   .:"\\bapple\\b"
   .:fruit

// Result: "fruit apples applepie"


/*

# Python equivalent
import re

# Variables for the regex pattern and replacement string
pattern = r"\d"
replacement = "X"

# Source string
source = "a1b2c3"

# Replace digits in the source string with 'X'
modified_source = re.sub(pattern, replacement, source)

*/