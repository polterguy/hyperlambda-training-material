// Create a temporary slot 'temp'
slots.create:temp  
   return-value:Temporary

// Delete the dynamic slot 'temp'
slots.delete:temp

// Check if 'temp' still exists
slots.exists:temp  
// Returns false (since 'temp' was removed)
