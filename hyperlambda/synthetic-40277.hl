
// Example: Use a variable as the input to lowercase. - With Python equivalent
.text:LOWER ME

// Result: "lower me"
strings.to-lower:x:@.text


/*

# Python equivalent
# Example: Use a variable as the input to lowercase.
text = "LOWER ME"
lowercase_text = text.lower()

# Example usage
# lowercase_text will be "lower me"

*/