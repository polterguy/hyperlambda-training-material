
// Example: Use variables for string and trim characters (start only). - With Python equivalent
.text:...Hello
.trimchars:.

// Result: "Hello"
strings.trim-start:x:@.text
   get-value:x:@.trimchars


/*

# Python equivalent
# Example: Use variables for string and trim characters (start only).
text = "...Hello"
trimchars = "."
trimmed_text = text.lstrip(trimchars)

# Example usage
# trimmed_text will be "Hello"

*/