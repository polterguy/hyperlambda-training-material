
// Example: Remove all non-digit characters from a string (keep only digits). - With Python equivalent
.mixed:"Phone: 555-1234!"
strings.replace-not-of:x:@.mixed
   .:0123456789
   .:

// Result: "5551234" (only digits remain)


/*

# Python equivalent
# Example: Remove all non-digit characters from a string (keep only digits).
import re

mixed_string = "Phone: 555-1234!"
digits_only_string = re.sub(r"[^\d]", "", mixed_string)

# Example usage
# digits_only_string will be "5551234"

*/