
// eq returns false if values are not equal or types differ - With Python equivalent
.a:5
.b:int:5
eq
   get-value:x:@.a
   get-value:x:@.b


/*

# Python equivalent
# eq returns false if values are not equal or types differ
a = "5"  # String type
b = 5    # Integer type

# Check if values are equal and types are the same
are_equal = (a == b)

# Example usage
# are_equal will be False because the types differ

*/