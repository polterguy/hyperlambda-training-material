
// neq returns true if two values are different - With Python equivalent
.src:int:5
neq
   get-value:x:@.src
   .:int:10


/*

# Python equivalent
# neq returns true if two values are different
src = 5

# Check if values are not equal
are_not_equal = (src != 10)

# Example usage
# are_not_equal will be True because 5 is not equal to 10

*/