
// Example: Split using a multi-character delimiter. - With Python equivalent
strings.split:foo||bar||baz
   .:||

/*
 * Resulting parts:
 * .:foo
 * .:bar
 * .:baz
 */


/*

# Python equivalent
# String to be split
string = "hello"

# Split the string on a comma (not present in the string)
split_string = string.split(",")

*/