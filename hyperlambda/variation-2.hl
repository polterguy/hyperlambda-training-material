// Loop for 20 iterations, logs a message each time
.no:int:0
while
   lt:x:@.no
      .:int:20
   .lambda

      // Logging iteration message
      log.info:"Howdy from while loop"
      math.increment:x:@.no

/*
Python equivalent:
# Run a loop for 20 iterations with logging
no = 0
while no < 20:
    print("Howdy from while")
    no += 1

*/