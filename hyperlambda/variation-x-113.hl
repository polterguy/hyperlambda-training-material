/*
 * This Hyperlambda snippet checks if there are NO child nodes using `not-exists`.
 * 
 * 1. The `.dest` node is created with a child node named `foo`.
 * 2. The `not-exists` slot is used to check if `.dest` has no children.
 * 3. Since `.dest` contains `foo`, the result will be `false`.
 */

.dest
   foo
not-exists:x:-/*
