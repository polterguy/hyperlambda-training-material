/*
 * This Hyperlambda snippet demonstrates behavior when no nodes match the query.
 *
 * 1. The `.data` node exists but has no matching children for `get-name`.
 * 2. The `get-name` slot retrieves nothing.
 * 3. The expected output is `null`.
 */

.data
get-name:x:@.data/foo
