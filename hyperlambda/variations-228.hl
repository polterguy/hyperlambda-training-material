// This example connects to the 'music_store' database and deletes an album record from the 'albums' table where the artist is 'The Beatles'.
sqlite.connect:music_store
   sqlite.delete
      table:albums
      where
         and
            artist:The Beatles