
// Generate a Hyperlambda snippet that copies nodes from [.input] and appends them into [.output].

// Copies nodes from [.input] and appends into [.output]
.input
   itemA:dataA
   itemB:dataB
   itemC:dataC
.output
add:x:@.output
   get-nodes:x:@.input/*
