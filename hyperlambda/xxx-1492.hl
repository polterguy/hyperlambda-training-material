
// Generate a Hyperlambda snippet that iterates through a list of users and logs their names.

// Iterates through a list of users and logs their names.
.users
   user1:Alice
   user2:Bob
   user3:Charlie
for-each:x:@.users/*

   // Logging each user name.
   log.info:x:@.dp/#
