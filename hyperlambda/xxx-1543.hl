
// Create a Hyperlambda snippet that inserts a 'task0' node before 'task1' inside '.tasks'.

// Inserts a "task0" node before "task1" inside ".tasks".
.tasks
   task1
insert-before:x:@.tasks/*/task1
   .
      task0:Preliminary task
