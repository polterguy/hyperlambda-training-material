
// Create a Hyperlambda script to ping the backend and verify the connection.
.description:Allows you to ping server to verify you are connected to your backend
.type:public
return-nodes
   result:success
