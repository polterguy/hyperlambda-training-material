
// Create a Hyperlambda script to transform the result of multiple actions/arguments into a new object. Use 'params' as input and return all nodes.
.arguments
   params
      type:key-value
      mandatory:bool:false
.icon:input

// Simply returning all nodes specified as input arguments.
return-nodes:x:@.arguments/*/*
