
// Create a Hyperlambda script to create a new item in a specified database and table using SQLite.
data.connect:DATABASE_NAME
   database-type:sqlite

   // Creating our record.
   data.create
      database-type:sqlite
      table:TABLE_NAME
      values
