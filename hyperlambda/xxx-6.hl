
// Create a Hyperlambda if condition that checks a variable 'foo'. Log a message if the condition is true.
if
   eq:x:@.arguments/*/foo
      .:bool:true
   .lambda

      // Condition is true.
      log.info:Condition is true
