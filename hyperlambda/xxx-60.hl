
// Create a Hyperlambda script to return items from a specified database and table using SQLite.
data.connect:DATABASE_NAME
   database-type:sqlite

   // Reading data from database.
   data.read
      database-type:sqlite
      table:TABLE_NAME
