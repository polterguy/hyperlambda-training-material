
// Create a Hyperlambda script to open a database connection and execute arbitrary SQL to return results as a record set using SQLite.
data.connect:DATABASE_NAME_HERE
   database-type:sqlite
   data.select:select * from TABLE_NAME_HERE
      database-type:sqlite
