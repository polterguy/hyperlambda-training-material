
// Create a Hyperlambda script that iterates over actions, sets names, adds nodes, and returns the result of all actions.
for-each:x:../*/execute
   set-name:x:./0/-/*/*
      get-value:x:@.dp/#
   add:x:./0/-/*/*
      get-nodes:x:@.dp/#/*
   add:x:../*/return
      .
         name
return
