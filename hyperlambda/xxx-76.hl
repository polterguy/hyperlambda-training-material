
// Create a Hyperlambda script to evaluate a lambda if a condition is true, with an else clause for when the condition is not true.
if
   eq:x:@SOME_EXPRESSION
      .:SOME_VALUE
   .lambda

      // Lambda evaluates if condition is true.
else

   // Lambda evaluated if condition is NOT true.
