
// Example: Use a variable for the string to encode. - With Python equivalent
.text:key=value&flag=true

// Result: "key%3Dvalue%26flag%3Dtrue"
strings.url-encode:x:@.text


/*

# Python equivalent
# Use a variable for the string to encode
import urllib.parse

text = "key=value&flag=true"
url_encoded_text = urllib.parse.quote(text)

*/