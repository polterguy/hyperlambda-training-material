// How do you run multiple SQL queries in a single [data.select] call and get multiple result sets?
data.connect:analytics
   data.select:select * from users; select * from events;
      multiple-result-sets:bool:true
