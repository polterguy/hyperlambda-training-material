// This snippet uses a numeric iterator to select the nth child of a node.
.numbers
   zero:0th
   one:1st
   two:2nd
get-value:x:@.numbers/1
// The numeric iterator '1' selects the second child of .numbers (index 1, node "one"), so get-value returns "1st".
